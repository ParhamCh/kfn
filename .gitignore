# Binaries and build output
# (build into ./bin via `go build -o bin/`; do not drop binaries in package dirs)
/bin/
/dist/
*.exe
*.out

# Test & coverage artifacts
*.test
coverage.out
coverage.html

# Go workspace files
go.work
go.work.sum

# Editor / OS noise
.idea/
.vscode/
*.swp
.DS_Store
